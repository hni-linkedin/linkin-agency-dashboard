export default function DashboardOverviewPage() {
  return (
    <div>
      <p
        style={{
          fontFamily: "var(--font-data)",
          fontSize: "var(--text-sm-size)",
          color: "var(--text-muted)",
        }}
      >
        Overview placeholder. Navigate via the sidebar or search (⌘K).
      </p>
    </div>
  );
}
